# Operations

This directory contains active and historical Flywheel work.

- `missions/` contains mission definitions and their goals.
- `records/` contains execution evidence and outcome records.
- `knowledge/` contains validated, reusable learning.

Application-related missions must not begin until `.flywheel/state.yaml` records `ready-for-missions`.
